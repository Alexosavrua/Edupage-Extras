node_modules/
dist/
*.log
.DS_Store
Thumbs.db
reference/
testing/
local-experiments/
CLAUDE.md
graphify-out

# Firefox/web-ext build output and packaged artifacts — generated by
# `npm run build:firefox`, never hand-edited, shouldn't be committed.
web-ext-artifacts/
*.xpi
*.zip

# Local tool config / secrets — never commit credentials.
.claude/
.env
.env.*
!.env.example
# Local-only research may contain saved page captures or school-specific notes.
# Keep it available to maintainers without risking it in a source package.
docs/
PROJECT_CONTEXT.md
AGENTS.md
